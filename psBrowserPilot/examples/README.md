# psBrowserPilot サンプルフロー

`*.json` ファイルは NodeFlow UI の「Load」ボタンから読み込めるグラフ定義です。

1. ブラウザ UI を開き、右上の **Load** をクリックします。
2. ファイルダイアログでサンプル JSON を選択します。
3. 必要に応じてノードの定数を編集し、PowerShell ブリッジを実行してください。

## 収録ファイル

- `sample-get-process-flow.json`: `sample_get_process` ノードを使って、指定したプロセス情報を取得する最小構成のフロー。
